# App
APP_NAME=Thesis Agent Backend
ENVIRONMENT=local
LOG_LEVEL=INFO

# Optional if you want to use it for uvicorn command
PORT=8000

# Database (preferred: DATABASE_URL)
# DATABASE_URL=postgresql+psycopg2://app:app@localhost:5432/app

# Or use POSTGRES_* parts
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=mike
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=postgres-db-vector

# pgvector
# Update to match your embedding model output dimension
# VECTOR_DIM is currently not wired into the ORM model (models.py uses Vector(768))
VECTOR_DIM=768
